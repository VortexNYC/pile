{
  "name": "pile-cli",
  "version": "0.1.0",
  "description": "CLI for Pile.",
  "license": "MIT",
  "bin": {
    "pile": "./bin/pile.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "prepack": "pnpm run build",
    "cli": "tsx src/index.ts"
  },
  "devDependencies": {
    "@pile/tsconfig": "workspace:*",
    "tsx": "4.23.13"
  }
}
